# longlive

Auto-synced backup of the Ableton × Claude live MIDI pipeline notebook and its
generated `outputs/` (MIDI JSON + interactive piano-roll HTML). Pushed every
30 minutes by a macOS launchd job (`com.leeny2or0.longlive`).
